import React from "react";
import {Card} from "@nextui-org/react";

interface PostItemProps extends BlogPost {

}

export const PostItem = (props: PostItemProps) => {
  return (
    <Card>

    </Card>
  )
}
